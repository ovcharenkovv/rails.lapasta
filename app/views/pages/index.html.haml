%h1 Listing pages

%table
  %tr
    %th Title
    %th Short body

  - @pages.each do |page|
    %tr
      %td= link_to page.title, short_post_url(@page_category.slug,page.slug)
      %td=raw page.short_body