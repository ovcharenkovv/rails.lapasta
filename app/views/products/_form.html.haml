-#= semantic_form_for [@category,@product], :html => { :multipart => true } do |f|
-#  = f.inputs do
-#    = f.input :published
-#    = f.input :title
-#    = f.input :price
-#    = f.input :supplier_price
-#    = f.input :supplier_id
-#    = f.input :category_id
-#    = f.input :status
-#    = f.input :description
-#    = f.inputs :attachments
-#    = f.inputs "Image or Video", :for => [:attachments, f.object.attachments || attachment.new ] do |fm|
-#      = fm.input :file, :for => :image, :as => :file, :hint => f.template.image_tag(f.object.attachment.image.url(:cropped))
-#  = f.actions do
-#    = f.action :submit, :as => :input
-#
= form_for [@category,@product], :html => { :multipart => true } do |f|
  -#= f.error_messages
  %p
    = f.label :title
    %br/
    = f.text_field :title
  %p
    = f.label :description
    %br/
    = f.text_area :description
  %h4 New Files
  =f.fields_for :attachments do |attachment_fields|
    - if attachment_fields.object.new_record?
      %p
        =attachment_fields.file_field :attachment

  %h4 Old Files
  = f.fields_for :attachments do |attachment_fields|
    - unless attachment_fields.object.new_record?
      .thumb
        = link_to image_tag(attachment_fields.object.attachment.url(:thumb)), attachment_fields.object.attachment.url(:original)
        = attachment_fields.check_box :_destroy
  %p= f.submit
